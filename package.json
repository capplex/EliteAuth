{
  "name": "eliteauth",
  "version": "1.0.0",
  "private": true,
  "description": "Open-source authentication and licensing platform",
  "scripts": {
    "dev": "npx http-server website -p 3000 -c-1",
    "check": "node --check website/app.js && node --check worker/worker.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/capplex/EliteAuth.git"
  }
}
